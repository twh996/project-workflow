# 架构说明

> 本文件记录**当前有效**的技术约定。被推翻的约定直接删掉，不保留历史。
> 每次开工，先读本文件和 `TASKS.md`。

## 项目是什么

<一句话说明这个项目做什么、给谁用>

## 技术选型

| 用途 | 选择 | 为什么选它 |
|---|---|---|
| 语言 | | |
| 框架 | | |
| 数据存储 | | |
| 运行环境 | | |

## 目录结构

```
<目录树，每个目录后面用一句话说明职责>
```

## 命名与代码风格

- <例：变量用下划线命名>
- <例：所有网络请求统一走 utils/request>

## 禁止事项

> 这一节是踩坑的归宿。任何「必须用 X 代替 Y」的经验都写在这里，写完就不用在对话里重复交代了。

- <例：不要用 A 库处理中文路径，Windows 下会乱码，统一用 B 库>

## 关键数据结构

> 被多个功能共享的表结构、接口格式写在这里。**改动前必须先更新本文件，再改代码。**

- <例：用户表字段：id / phone / created_at>

## 协作约定

1. 每次开工，先读 `ARCHITECTURE.md` 和 `TASKS.md`
2. 一个会话只做一个功能
3. 每次收工，必须更新 `TASKS.md` 并提交代码
4. 本文件只保留当前有效的约定，过期的直接删掉
