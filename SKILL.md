---
name: project-workflow
description: AI 协作开发的项目记忆工作流。在新项目开工、搭建项目文档骨架，或已建流程的项目里说"开工/收工/继续开发/做某功能/下一步做什么"时使用，负责建立并维护 ARCHITECTURE.md 与 TASKS.md。触发词：新项目、初始化项目、项目脚手架、开工、收工、存档进度、继续开发、下一步做什么、上下文快满了。
agent_created: true
---

# AI 协作开发工作流

核心原则：**对话是工作台，项目文件是仓库。** 不指望对话记住，一切靠文件交接。

## 模板使用（强制）

本 Skill 目录下 `templates/` 存放四份骨架文件：

```
templates/
├── ARCHITECTURE.md
├── TASKS.md
├── README.md
└── .gitignore
```

**建文档前必须实际读取对应模板文件，把信息填进占位符，不得凭记忆或自己的理解重新生成结构。** 填充时不得删除「协作约定」「禁止事项」「关键数据结构」三节。

## 场景判定

先判断属于哪种场景，再执行对应流程：

| 场景 | 特征 | 动作 |
|---|---|---|
| A 首次开工 | 项目根目录没有 ARCHITECTURE.md | 执行「A 建立脚手架」 |
| B 日常会话 | 已有 ARCHITECTURE.md | 执行「B 单功能开发」 |
| C 收工 | 用户说收工 / 存档 / 结束 | 执行「C 收工存档」 |
| D 上下文告急 | AI 开始忘事，或用户说上下文快满 | 立即执行「C 收工存档」，然后提示用户开新会话 |

**轻量任务直接做，不走流程**：改 bug、小调整、单轮问答，或预计改动 ≤2 个文件且不新增文件。走了流程反而拖慢节奏。

## A 建立脚手架

**先不要写任何业务代码。** 按顺序：

1. 先问用户 3-5 个必要问题，**一次问完，等回答后再动手**：项目做什么、给谁用、技术栈偏好、运行环境、有没有必须遵守的约束。
2. **读取本 Skill 目录 `templates/` 下的四份模板**，把第 1 步确认的信息填入占位符，写入项目根目录：
   - `ARCHITECTURE.md` — 选型及理由、目录结构、命名与风格约定、禁止事项、关键数据结构、协作约定
   - `TASKS.md` — 任务看板（进行中 / 待办 / 已完成）+ 下一步
   - `README.md` — 怎么运行、怎么验证
   - `.gitignore` — 按实际技术栈增补条目
3. 建立基础目录结构，只放占位文件，不写逻辑。
4. 初始化 git，`git status` 确认无多余文件后做第一次提交。
5. 确认 `ARCHITECTURE.md` 的「协作约定」一节保留了三条规则（填充时不得删除）：
   - 开工先读 `ARCHITECTURE.md` 和 `TASKS.md`
   - 一个会话只做一个功能
   - 收工必须更新 `TASKS.md` 并提交
6. 告诉用户：第一个该做的功能是什么，为什么先做它。

## B 单功能开发

1. 读 `ARCHITECTURE.md` 和 `TASKS.md`，**先向用户复述当前进度**，再动手。
2. 确认本次只做一个功能。用户一次要求多个时，主动提醒拆开。
3. 功能涉及的数据结构、接口格式，**先写进 `ARCHITECTURE.md` 再写代码**。
4. 做完后主动执行「C 收工存档」，不要等用户提醒。

## C 收工存档

按顺序做，做完逐条向用户确认：

1. 更新 `TASKS.md`：本次完成什么、卡在哪、下一步做什么；**已完成项只保留最近 5 条，更早的删掉**
2. 把本次踩的坑**提炼成约束**写进 `ARCHITECTURE.md` 的禁止事项，不要留在 `TASKS.md` 里当日志
3. **`git status` 预检**：确认没有临时文件、缓存、调试残留被混入。发现无关文件就补进 `.gitignore` 或清理掉，不要直接提交
4. 提交代码，写清本次做了什么
5. 告诉用户下一个会话该做什么

## 硬规则

- 建文档前必须实际读取 `templates/` 下的模板文件，不得凭记忆生成结构
- 填充模板时不得删除「协作约定」「禁止事项」「关键数据结构」三节
- 提交前必须 `git status` 预检，不提交无关文件
- 一个会话只做一个功能；功能做完主动提示用户开新会话
- 踩坑记录的归宿是「约束」而不是「日志」：能提炼成规则的写进 `ARCHITECTURE.md`，提炼不出来的直接删
- `ARCHITECTURE.md` 只保留当前有效的约定，被推翻的删掉，不保留历史
- 单个文档超过 150 行就该精简
- 多个会话并行会互相覆盖 `TASKS.md`，必须串行
